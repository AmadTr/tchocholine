<!DOCTYPE html>
<head>
	<meta charset="UTF-8"/>
	<title>
		{% block title %}
			Welcome!
		{% endblock %}
	</title>
	<link
	rel="icon" href="{{ asset('build/images/logoTchoChoLine.png') }}"/>
	{# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
	{% block stylesheets %}
		{{ encore_entry_link_tags('app') }}
	{% endblock %}
	{% block javascripts %}
		{{ encore_entry_script_tags('app') }}
	{% endblock %}
</head>
<nav class="navbar navbar-expand-lg navbar-light bg-light rounded" aria-label="Twelfth navbar example">
	<img src="{{ asset('build/images/logoTchoChoLine.png') }}" width="50">
	<div class="container-fluid">
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample10" aria-controls="navbarsExample10" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse justify-content-end" id="navbarsExample10">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('home') }}">
						Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				{% if is_granted('ROLE_ADMIN') %}

					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_admin') }}">Tableau de bord</a>
					</li>
					{% else %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_cart') }}">
						<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-cart4" viewbox="0 0 16 16">
							<path d="M0 2.5A.5.5 0 0 1 .5 2H2a.5.5 0 0 1 .485.379L2.89 4H14.5a.5.5 0 0 1 .485.621l-1.5 6A.5.5 0 0 1 13 11H4a.5.5 0 0 1-.485-.379L1.61 3H.5a.5.5 0 0 1-.5-.5zM3.14 5l.5 2H5V5H3.14zM6 5v2h2V5H6zm3 0v2h2V5H9zm3 0v2h1.36l.5-2H12zm1.11 3H12v2h.61l.5-2zM11 8H9v2h2V8zM8 8H6v2h2V8zM5 8H3.89l.5 2H5V8zm0 5a1 1 0 1 0 0 2 1 1 0 0 0 0-2zm-2 1a2 2 0 1 1 4 0 2 2 0 0 1-4 0zm9-1a1 1 0 1 0 0 2 1 1 0 0 0 0-2zm-2 1a2 2 0 1 1 4 0 2 2 0 0 1-4 0z"/>
						</svg>
						<sup class="badge badge-pill bg-danger">{{ getTotalQty() }}</sup>
					</a>
				</li>
				{% endif %}
				<li
					class="nav-item">
					{# <a class="nav-link" href="#">Mon compte</a> #}
				</li>
				{% if is_granted('ROLE_USER') and not is_granted('ROLE_ADMIN') %}

					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_customer_indexProfile',{'id':app.user.id}) }}">Profile</a>
					</li>
				{% endif %}

				{# {% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link" class="btn btn-outline-warning" href="{{ path('app_user_index') }}">Users</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" class="btn btn-outline-warning" href="{{ path('app_product_index') }}">Produits</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_cat_premier_index') }}">Catégorie_sup</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_category_index') }}">Catégories</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('all_order') }}">Commandes</a>
					</li>
				</li> #}
			{% if is_granted('ROLE_USER') and not is_granted('ROLE_ADMIN') %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('order_all') }}">Commandes</a>
				</li>
			{% endif %}
			{% if app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_logout') }}">Déconnexion ({{app.user.firstName}}
						{{app.user.lastname}})</a>
				</li>
			{% else %}
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_register') }}">Créer mon compte</a>
				</li>
			{% endif %}


		</ul>
	</div>
</div></nav><hr><nav class="navbar navbar-expand-lg navbar-light bg-light rounded" aria-label="Twelfth navbar example">
<div class="container-fluid">
	<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExample10" aria-controls="navbarsExample10" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse justify-content-md-center" id="navbarsExample10">
		{# <div class="container-fluid"> #}
		{# <div class="collapse navbar-collapse" id="navbarColor01"> #}
			{% if catSups is defined %}
				{% for catSup in catSups %}
				{# <div class="dropdown"> #}
					<ul class="navbar-nav me-auto"> <li class="nav-item dropdown">
					
						<a class="dropdown-toggle" id="dropdown01" data-bs-toggle="dropdown" aria-expanded="false">{{catSup.name}}</a>
				
						<ul class="dropdown-menu" aria-labelledby="dropdown01">
							{% for category in categories %}
								<li>
									<a class="dropdown-item" href="{{path('app_product_indexByCategory',{'cat':category.id})}}" id="dropdown01" data-bs-toggle="dropdown" aria-expanded="false">
										{{ category.name }}
									</a>
								</li>
							{% endfor %}
						</ul>
					</li>
				</ul>
				{# <li class="nav-item dropdown">
				            <a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-bs-toggle="dropdown" aria-expanded="false">Dropdown</a>
				            <ul class="dropdown-menu" aria-labelledby="dropdown01">
				              <li><a class="dropdown-item" href="#">Action</a></li>
				              <li><a class="dropdown-item" href="#">Another action</a></li>
				              <li><a class="dropdown-item" href="#">Something else here</a></li>
				            </ul>
				          </li> #}
				{# </div> #}
			{% endfor %}
			<a href="{{ path('app_blog') }}">Blog</a>
		{% endif %}
		{# </div> #}
		{# </div> #}
	</div>
</div></nav><hr><nav class="navbar navbar-expand-lg navbar-dark ">

<div class="dropdown">
	<button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false">Trier</button>
	<ul class="dropdown-menu" aria-labelledby="dropdownMenuButton1">
		<form class="d-flex" action="{{path('app_sort')}}" method="get">
			<li>
				<button class="dropdown-item" href="{{path('app_sort')}}" name="answer" value="croissant">Prix Croissant</button>
				<button class="dropdown-item" href="{{path('app_sort')}}" name="answer" value="décroissant">Prix Décroissant</button>
			</li>
		</form>
	</ul>
</div>
<form class="d-flex" method="get" action="{{path('home')}}">
	<input type="text" name="search" placeholder="Recherche">
	<button class="btn btn-secondary my-2 my-sm-0" type="submit">Recherche</button>
</form></nav>{% block body %}{% endblock %}
