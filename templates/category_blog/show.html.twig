{% extends 'base.html.twig' %}

{% block title %}CategoryBlog{% endblock %}

{% block body %}
    <h1>CategoryBlog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_blog.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_blog.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category_blog.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_blog_index') }}">back to list</a>

    <a href="{{ path('app_category_blog_edit', {'id': category_blog.id}) }}">edit</a>

    {{ include('category_blog/_delete_form.html.twig') }}
{% endblock %}
