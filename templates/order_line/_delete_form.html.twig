<form method="post" action="{{ path('app_order_line_delete', {'id': order_line.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer la ligne de commande?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order_line.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
